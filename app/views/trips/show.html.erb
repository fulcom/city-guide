
<div class="banner" style= "background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= image_path @trip.photo, crop: :fill %>')">
  <!-- ISA <img src="https://res.cloudinary.com/spark-and-rise-ltd/image/upload/v1566307014/photo-1531210483974-4f8c1f33fd35_xcfetf.jpg" alt="" class="avatar-banner card-trip-user"> -->
    <% guide_id = @trip.user_id %>
    <% guide = User.find(guide_id) %>
    <%= image_tag guide.picture, class: "avatar-banner card-trip-user" %>
</div>
<!-- <div class="banner-content d-flex mr-3 ml-3">
 -->  <div class="container">
  <div class="row">
    <div class="col-8">
      <div class="trip-presentation">
        <h1 class="text-left"><%= @trip.name %></h1>
        <p class="text-justify "><%= @trip.description %></p>
          <%= link_to "Back to all trips", trips_path, class: "btn btn-flat-violet w-40" %>
      </div>

    </div>
    <div class="col-4">
      <div class="trip-reservation">
      <!-- <div class="">
        <h1>
          <%# guide_id = @trip.user_id %>
          <%# guide = User.find(guide_id) %>
          <%#= guide.picture %>
        </h1>
      </div> -->
      <h2 class="text-center"> Réservez ce treep !</h2>
      <%= simple_form_for [@trip, @booking] do |f| %>
        <div class="form-inputs text-center">
            <%= f.input :start_date, as: :string,
                    required: false,
                    autofocus: true, input_html: {class: "datepicker"} %>
            <%= f.input :end_date, as: :string,
                    required: false, input_html: {class: "datepicker"} %>
        </div>
        <div class="form-actions">
             <%= f.button :submit, "Treep !", class: "btn btn-flat-green w-100"  %>
          </div>
          </div>
        <% end %>
    </div>
  </div>
</div>

